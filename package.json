{
  "name": "contact-form",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && cp infra/_headers dist/",
    "preview": "astro preview",
    "astro": "astro",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-10-07 -- astro dev",
    "pages:deploy": "astro build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^7.5.1",
    "@astrojs/tailwind": "^5.0.1",
    "astro": "^3.2.3",
    "flowbite": "^1.8.1",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "wrangler": "^3.11.0"
  }
}
